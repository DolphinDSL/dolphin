
t1 = during (2.seconds) { idle() }
t2 = t1

t3 = t1 >> t2

t =  t1 >> idle()

/*execute {
 during (1.seconds) {idle()} >> idle()
}
*/

execute {
   during (1.seconds) { idle() } >>
   during(1 seconds) { idle() }
}


